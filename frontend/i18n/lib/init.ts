/**
 * i18n initialization stub
 * This is a stub file to satisfy TypeScript imports.
 * Replace with actual i18n implementation when available.
 */

/**
 * Type representing paths that don't require parameters
 * Used for type-safe translation key references
 */
export type PathsWithNoParams = string;

/**
 * Initialize the i18n system
 */
export const initI18n = async (): Promise<void> => {
    // Stub implementation
};

export default initI18n;
